<h2>{{carWithTasks.make}} {{carWithTasks.model}}  -  {{carWithTasks.year}}</h2>
<section class="container">
    <section class="flex-row justify-center">
    {{#each carWithTasks.tasks as |task|}}
    {{> task-details}}
    {{/each}}
  </section>
</section>